# AGENTS.md

## Cursor Cloud specific instructions

This repository (`seylan-hub` / Seylan Hub) is currently empty — it contains only a `README.md` with no application code, dependencies, or configuration files.

- **No services** to start, build, lint, or test.
- **No package manager lockfile** or dependency manifest exists yet.
- When application code is added, update this section with build/run/test commands and any non-obvious caveats.
